<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.blog.system.mapper.SysProfileMapper">

    <resultMap id="SysUserResult" type="com.blog.common.core.domain.entity.Administrators">
        <id property="adminId" column="admin_id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="userType" column="user_type"/>
        <result property="description" column="description"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="lastLoginIp" column="last_login_ip"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="isDeleted" column="is_deleted"/>
    </resultMap>
    
    <sql id="selectAdministratorsVo">
        select admin_id,
               username,
               email,
               password,
               nickname,
               avatar,
               user_type,
               description,
               last_login_time,
               last_login_ip,
               create_time,
               update_time,
               create_by,
               update_by,
               is_deleted
        from administrators
    </sql>
    
    <update id="updateAvatarForAdmin">
        update administrators
        set avatar = #{url}
        where admin_id = #{adminId}
        limit 1
    </update>

    <update id="updateProfile" parameterType="updateProfile">
        update administrators
        <set>
            <if test="nickname != null and nickname != ''">
                nickname = #{nickname},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
        </set>
        where admin_id = #{adminId}
        limit 1
    </update>

    <update id="changePassword">
        update administrators
        set password = #{newPassword}
        where admin_id = #{adminId}
        limit 1
    </update>

    <select id="selectUserInfoForAdminByAdminId" resultMap="SysUserResult">
        <include refid="com.blog.system.mapper.SysUserMapper.selectAdminVo"/>
        where admin_id = #{adminId} and is_deleted = 0
    </select>
</mapper>